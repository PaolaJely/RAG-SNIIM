# =============================================================================
# Frontend — variables de entorno
# =============================================================================

# Desarrollo local con proxy Vite (pnpm dev): dejar vacío → /api relativo
# VITE_API_URL=

# Producción (Cloudflare Pages build): URL pública de la API en Render
# VITE_API_URL=https://rag-sniim-api.onrender.com
